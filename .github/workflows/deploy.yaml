name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
      - run: docker build -t gcelorrio/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true gcelorrio/react-test npm test

      - run: docker build -t gcelorrio/multi-client ./client
      - run: docker build -t gcelorrio/multi-nginx ./nginx
      - run: docker build -t gcelorrio/multi-server ./server
      - run: docker build -t gcelorrio/multi-worker ./worker

      - run: docker push gcelorrio/multi-client
      - run: docker push gcelorrio/multi-nginx
      - run: docker push gcelorrio/multi-server
      - run: docker push gcelorrio/multi-worker