version: '3'
services:
  client:
    image: 'gcelorrio/multi-client'
    mem_limit: 128m
    hostname: client
  server:
    image: 'gcelorrio/multi-server'
    mem_limit: 128m
    hostname: api
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
      - PGUSER=$PGUSER
      - PGHOST=$PGHOST
      - PGDATABASE=$PGDATABASE
      - PGPASSWORD=$PGPASSWORD
      - PGPORT=$PGPORT
  worker:
    image: 'gcelorrio/multi-worker'
    mem_limit: 128m
    hostname: worker
    environment:
      - REDIS_HOST=$REDIS_HOST
      - REDIS_PORT=$REDIS_PORT
  nginx:
    image: 'gcelorrio/multi-nginx'
    mem_limit: 128m
    hostname: nginx
    depends_on:
      - client
      - server
    ports:
      - '80:80'
